'use strict';

/**
 * admission-enquiry service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::admission-enquiry.admission-enquiry');
